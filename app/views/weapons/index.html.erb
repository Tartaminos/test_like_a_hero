<% @weapons.each do |weapon|%>
    <p> <%= weapon.name %> </p>
    <p> <%= weapon.currentpower.to_s %> </p>
    <p> <%= weapon.title %> </p>
    <p> <%= link_to 'Detalhes', weapon_path(weapon.id)  %> </p>
<% end %>